<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leidong.ldmart">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MyApplication">

        <!--欢迎界面-->
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--登陆界面-->
        <activity android:name=".ui.LoginActivity">
        </activity>

        <!--购买者主界面-->
        <activity android:name=".ui.MainBuyerActivity">
        </activity>

        <!--商家主界面-->
        <activity android:name=".ui.MainSellerActivity">
        </activity>

        <activity android:name=".ui.MainRootActivity">
        </activity>

        <!--客户看到的商品细节界面-->
        <activity android:name=".ui.ProductDetailActivity">
        </activity>

        <!--商户看到商品信息编辑界面-->
        <activity android:name=".ui.ProductManageActivity">
        </activity>

        <!--用户基本信息界面-->
        <activity android:name=".ui.UserActivity">
        </activity>

        <!--订单详情界面-->
        <activity android:name=".ui.OrderDetailActivity">
        </activity>
    </application>

</manifest>